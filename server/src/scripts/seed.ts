/**
 * Database seeding is disabled. Use the Admin app to create traits, programs, and brand voices.
 * To clear existing data and start from scratch, run: pnpm --filter @pmm/server reset-seed-data
 */
console.log("Seeding is disabled. Create traits, programs, and brand voices via the Admin app.");
process.exit(1);
