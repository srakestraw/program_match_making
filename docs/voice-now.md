# Voice NOW Notes

- Push-to-talk uses a muted audio track by default; track is enabled only while pressing the talk button.
- Transcript is rendered live in widget and sent to server incrementally.
- Realtime token endpoint uses server-side API key and returns ephemeral credentials to browser.
- SQLite db file is local at `server/prisma/dev.db`.
